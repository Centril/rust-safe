```rust,skt-main
#![feature(stmt_expr_attributes, proc_macro_hygiene)]
#![allow(unused_variables, dead_code, unused_imports)]

extern crate safe;
extern crate libc;

use safe::safe;
use libc::c_char;

fn main() {{
    {}
}}
```
